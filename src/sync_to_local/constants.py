"""String constants for sync-to-local."""

DEFAULT_SOURCE_TYPE = "nextcloud"
DEFAULT_RETRIES = 3
DEFAULT_TIMEOUT = 30
DEFAULT_LOG_LEVEL = "INFO"
DEFAULT_MANIFEST_FILENAME = ".sync-to-local-manifest.json"

MANIFEST_VERSION = 1
